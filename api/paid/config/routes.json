{
  "routes": [
    {
      "method": "GET",
      "path": "/paids",
      "handler": "paid.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/paids/count",
      "handler": "paid.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/paids/:id",
      "handler": "paid.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/paids",
      "handler": "paid.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/paids/:id",
      "handler": "paid.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/paids/:id",
      "handler": "paid.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
